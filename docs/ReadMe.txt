The page files at the root level will show automatically in the primary navigation. Others are in the _pages folder.


The build process takes a minute or two.

If anything goes wrong you can see it in the "Actions" tab in the github web UI.
